const namespace = '[UI]';

const UIActionTypes = {
   START_ACTION: `${namespace} START_ACTION`,
   STOP_ACTION: `${namespace} STOP_ACTION`,

   ALERT: `${namespace} ALERT`,
   SHOW_ALERT: `${namespace} SHOW_ALERT`,
   HIDE_ALERT: `${namespace} HIDE_ALERT`,
};

export default UIActionTypes;
