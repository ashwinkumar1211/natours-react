const namespace = '[TOUR]';

const TourActionTypes = {
   FETCH_TOURS: `${namespace} FETCH_TOURS`,
   FETCH_TOURS_FAILURE: `${namespace} FETCH_TOURS_FAILURE`,
   FETCH_TOURS_SUCCESS: `${namespace} FETCH_TOURS_SUCCESS`,
};

export default TourActionTypes;
